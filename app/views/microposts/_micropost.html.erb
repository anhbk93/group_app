<li id='micropost-<%= micropost.id %>'>
  <%= link_to gravatar_for(micropost.user, size: 50), micropost.user %>
  <span class='user'><%= link_to micropost.user.name, micropost.user %></span>
  <span class='content'>
    <%= micropost.content %>
    <% default_image = 'http://www.propertybankevents.com/wp-content/uploads/2015/06/no.png' %>
    <% random_image = 'http://lorempixel.com/400/300/technics/' %>
    <% if micropost.picture? %>
      <%= image_tag micropost.picture.url %>
    <% else %>
      <%= image_tag random_image %>
    <% end %>
  </span>
  <span class='timestamp'>
    Posted <%= time_ago_in_words(micropost.created_at) %> ago.
    <% if current_user?(micropost.user) %>
      <%= link_to 'delete', micropost, method: :delete,
                                       data: { confirm: 'You sure?' } %>
    <% end %>
  </span>
  
  <div id="comment">
    <%= form_for([micropost, micropost.comments.build]) do |f| %>
    <p>
      <br>
      <%= f.text_area :comment, id: 'textarea_review'%>
    </p>
    <p>
      <%= f.submit "Create review", id: "botton", class: "btn btn-success green" %>
    </p>
    <% end %>
  </div>
  <div id="reviews">   
    <%= render 'comment/comment', micropost: micropost %>
  </div>
</li>